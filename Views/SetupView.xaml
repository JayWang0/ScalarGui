<UserControl x:Class="ScalarGui.Views.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ScalarGui.ViewModels"
             xmlns:conv="clr-namespace:ScalarGui.Converters"
             xmlns:loc="clr-namespace:ScalarGui.Localization"
             d:DataContext="{d:DesignInstance Type=vm:SetupViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBoolConverter x:Key="InverseBool"/>

        <Style x:Key="ToolCardStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#D0D7DE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="{loc:Str Setup_Title}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock Text="{loc:Str Setup_Subtitle}" Foreground="Gray" Margin="0,0,0,12"/>

            <GroupBox Header="{loc:Str Setup_RequiredTools}">
                <StackPanel Margin="4">
                    <Border Style="{StaticResource ToolCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" VerticalAlignment="Top" Margin="0,0,12,0" FontSize="22" FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="❌"/>
                                        <Setter Property="Foreground" Value="#D13438"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsGitInstalled}" Value="True">
                                                <Setter Property="Text" Value="✅"/>
                                                <Setter Property="Foreground" Value="#107C10"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{loc:Str Setup_Git}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="{loc:Str Setup_GitDesc}"
                                           Foreground="Gray" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding GitVersion}" Foreground="#0F6CBD" FontWeight="SemiBold" Margin="0,8,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsGitInstalled}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>

                            <Button Grid.Column="2" Content="{loc:Str Setup_Download}" Command="{Binding OpenGitDownloadCommand}" Padding="14,6" Margin="12,0,0,0" VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsGitInstalled}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource ToolCardStyle}" Margin="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" VerticalAlignment="Top" Margin="0,0,12,0" FontSize="22" FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="❌"/>
                                        <Setter Property="Foreground" Value="#D13438"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsScalarInstalled}" Value="True">
                                                <Setter Property="Text" Value="✅"/>
                                                <Setter Property="Foreground" Value="#107C10"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{loc:Str Setup_Scalar}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="{loc:Str Setup_ScalarDesc}"
                                           Foreground="Gray" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding ScalarVersion}" Foreground="#0F6CBD" FontWeight="SemiBold" Margin="0,8,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsScalarInstalled}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>

                            <Button Grid.Column="2" Content="{loc:Str Setup_Download}" Command="{Binding OpenScalarDownloadCommand}" Padding="14,6" Margin="12,0,0,0" VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsScalarInstalled}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>
                </StackPanel>
            </GroupBox>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="{loc:Str Setup_Recheck}" Command="{Binding CheckPrerequisitesCommand}"
                        IsEnabled="{Binding IsChecking, Converter={StaticResource InverseBool}}"
                        Padding="16,6" Background="#0078D4" Foreground="White" FontWeight="SemiBold"/>
                <ProgressBar Width="120" Height="6" Margin="12,0,0,0" VerticalAlignment="Center"
                             IsIndeterminate="True"
                             Visibility="{Binding IsChecking, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>

            <Border Margin="0,12,0,0" Padding="12" CornerRadius="8" BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#FFF4CE"/>
                        <Setter Property="BorderBrush" Value="#FFB900"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding AllToolsReady}" Value="True">
                                <Setter Property="Background" Value="#DFF6DD"/>
                                <Setter Property="BorderBrush" Value="#107C10"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsChecking}" Value="True">
                                <Setter Property="Background" Value="#E5F1FB"/>
                                <Setter Property="BorderBrush" Value="#0078D4"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold" TextWrapping="Wrap"/>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
